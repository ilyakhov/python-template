# Python Project Template (VS Code + Conda)

This repo is a template for new Python projects with consistent VS Code setup.

## One-time machine setup
Set an env var pointing to your conda python:

```bash
export CONDA_PYTHON="/Users/igor/opt/miniconda3/envs/ds/bin/python3"
```
